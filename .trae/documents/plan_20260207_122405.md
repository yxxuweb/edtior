您提得非常对。作为一个企业级的低代码核心库，目录结构不仅要包含代码，还需要包含工程化配置、文档、示例以及更细粒度的模块划分。

我将对 `ARCHITECTURE.md` 中的目录结构部分进行**全景式补充**，按 **Monorepo 工程结构** 和 **核心包内部结构** 两个维度进行详细展开。

### 主要补充内容

1. **Monorepo 工程全景**：

   * 补充 `scripts` (构建脚本)、`examples` (多框架示例)、`.github` (CI/CD) 等工程目录。

   * 明确 `packages` 下的包划分：`core` (内核)、`designer` (交互层)、`renderer-*` (渲染适配器)。建议将**交互逻辑 (Designer)** 从纯数据内核中独立出来，保持 Core 的纯净。

2. **Core 包深度细化**：

   * **`kernel/`**: 细分为 `model` (数据定义) 和 `system` (事务、历史等服务)。

   * **`drivers/`**: 新增输入驱动模块 (键盘、鼠标、拖拽事件标准化)。

   * **`events/`**: 独立的事件总线模块。

   * **`api/`**: 统一的对外导出层。

3. **规范说明**：

   * 增加对每个关键目录职责的详细注释。

### 预期文档结构预览

```markdown
## 6. 目录结构规范 (Directory Structure)

### 6.1 Monorepo 工程全景
/
├── packages/
│   ├── core/               # [核心] 数据模型、逻辑内核 (无 UI 依赖)
│   ├── designer/           # [交互] 画布交互、拖拽、辅助线 (DOM 相关)
│   ├── renderer-react/     # [适配] React 渲染器
│   ├── renderer-vue/       # [适配] Vue 渲染器
│   └── plugin-logger/      # [插件] 官方插件示例
├── examples/               # 演示项目
├── scripts/                # 构建脚本
...

### 6.2 @lowcode/core 内部结构
packages/core/src/
├── kernel/                 # 内核层
│   ├── model/              # 数据模型 (Node, Document, Selection)
│   ├── system/             # 系统服务 (Engine, History, Transaction)
├── drivers/                # 驱动层 (DragDrop, Keyboard, Mouse)
...
```

我将立即更新文档。
