您指出的缺失非常关键，这三个部分确实是低代码编辑器最核心、最复杂的实现细节。

我已规划了以下补充方案，将**新增 2 份文档**并**深度修订 1 份文档**来覆盖这些需求：

### 1. 新增：布局系统设计 (Layout System)
*   **文档**: `006-layout-system.md`
*   **内容**:
    *   **流式布局 (Flow)**: 基于 Flex/Block 的文档流排版策略。
    *   **网格布局 (Grid)**: 基于 CSS Grid 或 12/24 栅格系统的排版策略。
    *   **自由布局 (Free)**: 基于绝对定位 (Absolute) 的拖拽与对齐策略。
    *   **布局策略接口**: 定义 `LayoutStrategy` 接口，如何计算节点位置和尺寸。

### 2. 新增：视图渲染机制 (View Renderer)
*   **文档**: `007-view-renderer.md`
*   **内容**:
    *   **Schema Hydration**: 详述如何将静态 JSON Schema 递归解析为内存中的 `Node Tree` (包括 ID 生成、引用处理)。
    *   **Recursive Rendering**: 详述渲染器如何遍历 Node Tree，动态查找组件实现，并挂载到页面。
    *   **Update Strategy**: 当 `Node` 属性变化时，如何精确更新对应的视图组件（避免全量重渲染）。

### 3. 修订：交互驱动设计 (Interaction Drivers)
*   **文档**: `005-interaction-drivers.md`
*   **内容**: 增加 **“拖拽策略 (Drag Policies)”** 章节，详细解答您的疑问：
    *   **画布内拖拽 (Sort/Move)**: 页面组件的自由移动或流式排序。
    *   **容器内拖拽 (Nested Drop)**: 如何判定鼠标进入了某个容器？如何计算插入位置（Insertion Index）？
    *   **占位符 (Placeholder)**: 拖拽过程中，如何显示“即将放入这里”的视觉提示。

---

**执行顺序**：
1.  创建 `006-layout-system.md`。
2.  创建 `007-view-renderer.md`。
3.  更新 `005-interaction-drivers.md`。
4.  更新 `README.md` 索引。

这个计划是否覆盖了您的需求？
